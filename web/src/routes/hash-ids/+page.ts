// Static, keyword-rich reference content — prerendered to real HTML like /about
// so it's indexable and shareable without executing the SPA. Overrides the
// app-wide ssr=false / prerender=false defaults.
export const ssr = true;
export const prerender = true;
